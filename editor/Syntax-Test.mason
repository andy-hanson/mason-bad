use arithmetic for +
use graceful-fs~fs

\ comment

123.45

pi:Number = 3.14
pi:Number. 3.14

|:Number x:Number \ a function
	+ x 3


""

"One plus one is {1 + 1}!"

"
	It's still {1 + 1}...


count-string. |a
	case a
		0
			"zero"
		1
			"one"
		if < a 10
			"some"
		:Number
			"a lot"
		else
			"not a number"
