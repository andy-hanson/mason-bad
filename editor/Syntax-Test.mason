use arithmetic for +
use graceful-fs~fs

()[]{}

""
"\"" was a string
"One plus one is {1 + 1}!"
"
	It's still {1 + 1}...
	\{not an interpolation}

\\\
comment
\\\

123.45

pi:Number~pie pi:Number~pie = 3.14
pi:Number~pie pi:Number~pie pi:Number~pie. 3.14

|:Number x:Number
	+ x 3


count-string. |a
	case a
		0
			"zero"
		1
			"one"
		if < a 10
			"some"
		:Number
			"a lot"
		else
			"not a number"
